# GenZFit SMPL-X Avatar Backend — Python 3.10+
# Install CPU-only PyTorch first (no CUDA required):
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
# Then install remaining packages:
#   pip install -r requirements.txt

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4

# --- Scientific / numerics ---
numpy==1.26.4

# --- 3D mesh generation ---
trimesh==4.4.1

# --- Image processing (UV texture application) ---
Pillow==10.3.0
opencv-python==4.10.0.82

# --- SMPL-X parametric body model ---
# Requires the .pkl model files in models/smplx/
# (SMPLX_MALE.pkl, SMPLX_FEMALE.pkl, SMPLX_NEUTRAL.pkl)
smplx==0.1.28

# --- Deep learning runtime (CPU only) ---
# Install separately with the --index-url flag shown above
torch==2.3.0

# --- Utilities ---
python-dotenv==1.0.1
